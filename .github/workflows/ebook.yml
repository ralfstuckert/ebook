name: EBook

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    
      - name: Checkout dvag/maven-build-action
        uses: actions/checkout@v2
        with:
          repository: ralfstuckert/pdfbox-layout.wiki
          ref: refs/heads/master
          # GitHub's personal access token with access to `dvag/maven-build-action`
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: false
          path: ./wiki
        env:
          GITHUB_ACTOR: ${{ env.GITHUB_ACTOR }}
          
      - name: create file list
        id: files_list
        run: |
          mkdir output 
          cd wiki
          rm -f _Sidebar.md
          echo "::set-output name=files::$(printf '"%s" ' *.md)"

      - uses: docker://pandoc/latex:2.9
        with:
          args: --output=output/result.epub ${{ steps.files_list.outputs.files }}
          
      - uses: actions/upload-artifact@master
        with:
          name: output
          path: output
